const base_url = 'http://127.0.0.1:6001/';

const APIs = {
    get_chatbot_respond: base_url + 'phi3',
    upload_file: base_url + 'uploadfile',
    get_pdf_respond: base_url + 'pdf_reader'
};

export default APIs;